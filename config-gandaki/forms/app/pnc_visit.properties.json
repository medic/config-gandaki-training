{
    "title": [
      {
        "locale": "en",
        "content": "PNC visit"
      },
      {
        "locale": "ne",
        "content":"पीएनसी जाँच"
      }
    ],
    "context": {
      "place": false,
      "person": true,
      "expression": "contact.type === 'person' && contact.sex !== 'male' && ageInYears(contact) >= 15 && summary.is_pnc_eligible && !contact.muted && !contact.date_of_death"
    },
    "icon": "icon-pregnancy"
  }
